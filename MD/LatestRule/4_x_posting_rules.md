# SNS (X/Twitter) 運用ルール
**最終更新日：** 2026年4月30日
**アカウント：** @TooNScriptStore (カブ先生)

---

## 1. 基本方針 (アルゴリズム対策)
- **URLは親ツイートに貼らない：** 外部リンクを含むツイートは表示回数 (インプレッション) が激減するため。
- **リプライツリー形式：** 1枚目のツイートで惹きつけ、リプライ欄で詳細解説やURL誘導を行う。
- **プロフ誘導：** 「続きはプロフのリンクから👇」という誘導も有効。


## 2. キャラクター設定 (カブ先生)
- **一人称：** わし
- **笑い方：** フォッフォッフォ！
- **語尾：** 〜じゃ、〜のう、〜ぞい
- **性格：** お金に詳しいが、少しお茶目な老先生。

## 3. 投稿テンプレート

### パターンA：コンテンツ紹介 (リプライ誘導)
- **親ツイート：** 図解画像 + 「投資のキホンを教えるぞい！今日は〇〇についてじゃ。」
- **リプライ：** 解説の補足 + 記事URL + ハッシュタグ

### パターンB：マインド系 (画像のみ)
- **内容：** 投資の心構えや、暴落時の落ち着かせ方。
- **目的：** 共感を得てフォローや「いいね」を増やす。

## 5. AI Workstation 連携ワークフロー (推奨)
手動での API 実行ではなく、`AI Workstation` (Webツール) を使用して投稿内容を確認・修正してから投稿する流れを基本とする。

1. **ドラフト作成:** `D:\Antigravity\Work Smart\x_dashboard\draft.json` を更新する。
   - `kabu` セクションの `posts` (4要素の配列) に投稿内容を書き込む。
   - `image` にアイキャッチの絶対パス（例: `D:\Antigravity\Kabu\image\column\ColumnYYYYMMDD.png`）を記述。
2. **ツール同期:** ユーザーに `AI Workstation` の **X Post Manager** で「カブ先生」を選択し、「案を同期」をクリックするよう依頼する。
3. **内容確認・投稿:** ユーザーがツール上で最終確認（必要なら修正）を行い、「投稿実行」をクリックして完了。

## 6. 注意事項
- **太字禁止:** ツイート本文に `**` などのマークダウン装飾は含めない（X上でそのまま表示されてしまうため）。
- **ハッシュタグ:** 親ツイートに2〜3個含めるのが望ましい。

## 7. 自動化（Playwright）時の技術的注意点
- **要素のクリックエラー（最重要）:** X（Twitter）の画面上には、透明なポップアップや「ツイートを送信しました」というトースト通知が前面に被さることが頻繁にあります。
- これによりPlaywright標準の `element.click()` や `click(force=True)` が `subtree intercepts pointer events` (Timeout 30000ms exceeded) で失敗する原因となります。
- **解決策:** Xの画面上でボタン（投稿ボタン、リプライボタン、入力エリアなど）をクリックさせるスクリプトを書く際は、必ずJavaScriptの内部DOMから直接クリックを発火させる **`element.evaluate("node => node.click()")`** を使用してください。Playwright標準のクリックメソッドは極力避けてください。

## 8. xurl コマンドによる API 実行ルール
本PCには複数のXアカウント（カブ先生、黒焦団）の認証情報が設定されています。コマンドラインや自動化スクリプト等で `xurl` を使ってAPI操作を行う際は、意図しないアカウントへの誤投稿（誤爆）を防ぐため、**必ず `--app Kabu_X` を付与してアカウントを明示**してください。

* **ツイート投稿例**:
  ```powershell
  npx @xdevplatform/xurl --app Kabu_X post "カブ先生からの投稿内容"
  ```
* **MCPサーバー起動例**:
  ```powershell
  npx @xdevplatform/xurl --app Kabu_X mcp https://api.x.com/mcp
  ```
* **注意**: `--app` オプションを省略すると、デフォルト設定の状況によって別のアカウントで動作するリスクがあります。
